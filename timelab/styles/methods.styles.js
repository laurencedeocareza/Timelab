import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: 'light gray', // Light gray background
},
  text: {
    fontSize: 20,
    fontWeight: 'bold',
    color: '#333', // Dark gray color
  },
});
